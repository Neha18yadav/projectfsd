{
  "name": "fullstacket",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -k \"npm --prefix Backend run dev\" \"npm --prefix frontend start\" \"wait-on http://localhost:3000 && open -a \\\"Google Chrome\\\" http://localhost:3000 && sleep 99999\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.2"
  }
}
